# Copy this file to .env.local and replace with your actual keys

# Supabase Configuration (for authentication only)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google Maps API (optional - for enhanced address validation)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Security Note: Never commit .env.local to version control
# The app uses the US Census API for political data (no API key required)

# Instructions:
# 1. Go to https://supabase.com/dashboard/project/lnwxgwllzoydhjglxume/settings/api
# 2. Copy the "anon public" key and replace YOUR_ACTUAL_ANON_KEY_HERE
# 3. Copy the "service_role" key and replace YOUR_ACTUAL_SERVICE_ROLE_KEY_HERE
# 4. Save this file as .env.local (remove the EXAMPLE. prefix)
# 5. Restart your development server: npm run dev 

# Google APIs
NEXT_PUBLIC_GOOGLE_CIVIC_API_KEY=your_google_civic_api_key

# Cron Job Security
CRON_SECRET=your_secure_random_string_for_cron_jobs

# Optional: News API (if using news features)
NEWS_API_KEY=your_news_api_key

# Optional: OpenAI API (if using AI features)
OPENAI_API_KEY=your_openai_api_key 